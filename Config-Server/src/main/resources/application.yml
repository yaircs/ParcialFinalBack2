server:
  port: ${PORT:8888}

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yaircs/ParcialFinalBack2.git
          default-label: main
          force-pull: true
          search-paths: configuration

